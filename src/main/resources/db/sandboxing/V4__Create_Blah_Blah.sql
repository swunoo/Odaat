-- users_table.sql
CREATE TABLE new (
    id serial PRIMARY KEY,
    street varchar(50) NOT NULL,
    phone int
);

-- users_table.sql
CREATE TABLE newer (
    id serial PRIMARY KEY,
    street varchar(50) NOT NULL,
    phone int
);

-- users_table.sql
CREATE TABLE newest (
    id serial PRIMARY KEY,
    street varchar(50) NOT NULL,
    phone int
);