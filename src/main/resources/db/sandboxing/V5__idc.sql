-- users_table.sql
CREATE TABLE a (
    id serial PRIMARY KEY,
    street varchar(50) NOT NULL,
    phone int
);

-- users_table.sql
CREATE TABLE b (
    id serial PRIMARY KEY,
    street varchar(50) NOT NULL,
    phone int
);

-- users_table.sql
CREATE TABLE c (
    id serial PRIMARY KEY,
    street varchar(50) NOT NULL,
    phone int
);